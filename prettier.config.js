export default {
  semi: true, // Met un point-virgule à la fin de chaque instruction
  trailingComma: "none", // N’ajoute PAS de virgule après le dernier élément (ex: tableaux/objets)
  singleQuote: true, // Utilise des apostrophes (simples quotes : ') au lieu des guillemets doubles (")
  printWidth: 100, // Limite la longueur d’une ligne à 100 caractères max
  tabWidth: 2, // Utilise 2 espaces pour chaque niveau d’indentation
  useTabs: false, // Indente avec des espaces (et non des tabulations)
};